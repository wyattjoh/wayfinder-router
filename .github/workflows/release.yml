name: Release

# Publish to PyPI via Trusted Publishing (OIDC) when a version tag is pushed,
# either v-prefixed (v2026.6.3) or a bare CalVer tag (2026.6.3). No API token or
# password is stored: PyPI verifies the workflow's identity (owner/repo/workflow +
# the `pypi` environment) against its trusted publisher.
on:
  push:
    tags:
      - "v*"        # v-prefixed, e.g. v2026.6.3 or v0.1.6
      - "[0-9]*"    # bare CalVer, e.g. 2026.6.3

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Verify the tag matches the package version
        run: |
          tag="${GITHUB_REF_NAME#v}"   # strip an optional leading v; bare tags pass through
          ver="$(python -c 'import wayfinder_router; print(wayfinder_router.__version__)')"
          if [ "$tag" != "$ver" ]; then
            echo "::error::tag $GITHUB_REF_NAME does not match package version $ver"
            exit 1
          fi
          echo "publishing wayfinder-router $ver"
      - name: Build sdist and wheel
        run: |
          python -m pip install --upgrade pip build
          python -m build
      - name: Check distribution metadata
        run: |
          pip install twine
          twine check dist/*
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write  # required for Trusted Publishing (OIDC)
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  rust-binary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Build Rust CLI artifact
        run: cargo build --release --bin wayfinder-router
      - name: Smoke-test Rust TUI command
        run: ./target/release/wayfinder-router chat --dry-run "hello"
      - uses: actions/upload-artifact@v4
        with:
          name: wayfinder-router-linux-x86_64
          path: target/release/wayfinder-router
